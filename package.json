{
  "name": "mnd-le-plan",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo Build complete — static site, no compilation needed.",
    "start": "python3 -m http.server 3000"
  }
}
